using System;

namespace GvasFormat.Serialization.Binary
{
    class SerializationConstant
    {
        internal const long I64_Size_FloatProperty = sizeof(Single);
        internal const long I64_Size_IntProperty = sizeof(Int32);
    }
}
